# Frozen source-to-Lean correspondence review

Exact declarations are qualified by `Percolation.` below. Exact types were inspected in their
production files at commit `7a0cece`; application files re-state the full public types and compile
against them. All source-facing declarations have the transitive axiom set
`[propext, Classical.choice, Quot.sound]` unless the final independent report records otherwise.

| Source | Precise source-facing content and conventions | Lean declaration(s) | Dependencies / divergence | Cases and adversarial evidence | Comparator | Producer verdict |
|---|---|---|---|---|---|---|
| 6.1, (6.2), pp.117–120 | `d>=2`, finite susceptibility; a positive rate bounds every box-radius tail, non-strict at `n=0` | `boxRadiusTail_exponential_decay_of_susceptibility_lt_top` | box quantity is explicitly L-infinity; no extra public `p<1` | preliminary `Theorem61.lean`; strict `n=0` anti-target refuted | prepared/not run | `PASS` |
| 6.10, pp.120–124 | positive density; logarithmic limit and dimension-only two-sided polynomial constants for positive `n` | `boxRadiusTail_logRate_tendsto`, `boxRadiusTail_twoSided_decay` | denominator form is equivalent to the printed negative power and avoids zero junk | preliminary `Theorem610.lean` | prepared/not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.14, (6.40), pp.121–125 | continuity/antitonicity, strict subcritical decrease, zero-density divergence, critical zero; ratio comparison for `b<1` | five `boxRadiusDecayRate_*` declarations in `AxiomAudit.lean` | endpoint-safe ENNReal extension is separate; (6.40) records necessary nonzero denominator | preliminary `Theorem614.lean` | prepared/not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.44, pp.126 | axis two-point rate equals box rate; constant precedes density and lower factor is one `p` | `twoPointConnectivity_axis_logRate_tendsto`, `twoPointConnectivity_axis_twoSided_decay` | positive `n` explicit | preliminary `Theorem644.lean` | prepared/not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.47, pp.126–127 | arbitrary point upper bound and nonzero-point polynomial lower bound with `p^(d|x|)` | `twoPointConnectivity_twoSided_decay` | source lower display is undefined at `x=0`; corrected hypothesis explicit | preliminary `Proposition647.lean`, `AntiTargets.lean` | prepared/not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.49, pp.127–128 | geometric two-point bound, endpoint-safe `xi<=chi`, and left divergence of susceptibility | three declarations in `Proposition649.lean` | correlation length uses ENNReal, giving infinity at criticality | preliminary `Proposition649.lean` | prepared/not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.75, (6.77), p.132 | exact bound above `n>chi^2`; subcritical exponential decay for sufficiently large `n` | `clusterSizeAtLeast_le_two_exp_neg`, `clusterSizeAtLeast_exponential_decay_of_lt_critical` | printed all-`n>=1` strict corollary is false at `n=1` | `cases/TailAndRates.lean`, `counterexamples/AntiTargets.lean` | producer draft/not run | `FAIL_SOURCE_LITERAL`; reject the false printed corollary and review the corrected eventual theorem separately |
| 6.78, (6.80), (6.82)–(6.83), pp.132–134 | exact-size log rate, exact finite-index prefactor, finite-tail rate, positivity below criticality, `zeta<=phi` | five rate declarations in `TailAndRates.lean`/`AxiomAudit.lean` | finite tail deliberately excludes infinite clusters; ordinary tail identified under `theta=0` | `cases/TailAndRates.lean` | prepared/not run | `PASS` |
| 6.87, pp.134–135 | finite nonempty terminal set has a removable terminal preserving connection among the rest | `exists_terminal_deletion_preserves_connected` | finite `Finset` corrects a false infinite-set reading; ambient graph remains arbitrary | preliminary `Lemmas687689.lean`; integer-path anti-target documented | producer draft/not run | `FAIL_SOURCE_LITERAL`; reject the false arbitrary-set reading and review the finite correction separately |
| 6.89, pp.135–136 | three-point probability bounded by a sum of triple two-point products | `threePointConnectivity_le_tsum_prod` | ENNReal sum prevents nonsummable-real-tsum junk at endpoints | preliminary `Lemmas687689.lean` | prepared/not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.93–6.97, pp.136–138 | general skeleton bound, exact skeleton enumeration, moment bound and square-root exponential moment | `multiPointConnectivity_le_skeleton_sum`, `connectivitySkeletonCount_eq_doubleFactorial`, `clusterSizeMoment_le`, `clusterSize_expMoment_le` | at this revision the count was defined by the desired closed form, and (6.97) bounded a custom series without identifying it with the literal expectation | `cases/TreeMoments.lean`, counts `1,3,15` | producer draft/not run | `FAIL_STATEMENT`; repaired after this frozen revision |
| 6.102, pp.139–141 | normalized exact probabilities are supermultiplicative with exact factor `p(1-p)^-2` for positive sizes and `0<p<1` | `CubicBondAnimal.finiteClusterSizeProbability_normalized_supermultiplicative` | no change; normalization obtained by rooted/pointed-anchored equivalence | `cases/TailAndRates.lean` | prepared/not run | `PASS` |
| 6.108, pp.142–145 | cluster density and susceptibility are real analytic on `[0,p_c)`, via complex animal series agreeing with physical functions | `concreteClusterDensitySeries_analyticOnNhd_belowCritical`, `concreteSusceptibilitySeries_analyticOnNhd_belowCritical`, combined theorem | real `AnalyticOnNhd` includes genuine neighborhoods; endpoint and interior complex majorants are separate | `cases/Analyticity.lean`, endpoint analytic anti-target guard | prepared/not run | `PASS` |

This first pass is frozen as a failed review. It also found the missing correlation-length
continuity/strict-monotonicity/zero-density conclusions and incomplete challenges for 6.78 and
6.108. “Producer draft/not run” is not a Comparator pass.
