# Frozen source-to-Lean correspondence review

Exact declarations are qualified by `Percolation.` below. Exact types were inspected in their
production files at final repair commit `c4286f9`; application files re-state the full public types and compile
against them. All source-facing declarations have the transitive axiom set
`[propext, Classical.choice, Quot.sound]` unless the final independent report records otherwise.

| Source | Precise source-facing content and conventions | Lean declaration(s) | Dependencies / divergence | Cases and adversarial evidence | Comparator | Independent verdict |
|---|---|---|---|---|---|---|
| 6.1, (6.2), pp.117–120 | `d>=2`, finite susceptibility; a positive rate bounds every box-radius tail, non-strict at `n=0` | `boxRadiusTail_exponential_decay_of_susceptibility_lt_top` | box quantity is explicitly L-infinity; no extra public `p<1` | preliminary `Theorem61.lean`; strict `n=0` anti-target refuted | not run | `PASS` |
| 6.10, pp.120–124 | positive density; logarithmic limit and dimension-only two-sided polynomial constants for positive `n` | `boxRadiusTail_logRate_tendsto`, `boxRadiusTail_twoSided_decay` | denominator form is equivalent to the printed negative power and avoids zero junk | preliminary `Theorem610.lean` | not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.14, (6.40), pp.121–125 | continuity/antitonicity, strict subcritical decrease, zero-density divergence, critical zero; ratio comparison for `b<1` | five `boxRadiusDecayRate_*` declarations in `AxiomAudit.lean` | endpoint-safe ENNReal extension is separate; (6.40) records necessary nonzero denominator | preliminary `Theorem614.lean` | not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.44, pp.126 | axis two-point rate equals box rate; constant precedes density and lower factor is one `p` | `twoPointConnectivity_axis_logRate_tendsto`, `twoPointConnectivity_axis_twoSided_decay` | positive `n` explicit | preliminary `Theorem644.lean` | not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.47, pp.126–127 | arbitrary point upper bound and nonzero-point polynomial lower bound with `p^(d|x|)` | `twoPointConnectivity_twoSided_decay` | source lower display is undefined at `x=0`; corrected hypothesis explicit | preliminary `Proposition647.lean`, `AntiTargets.lean` | not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.49 and correlation-length consequences, pp.127–128 | geometric two-point bound, endpoint-safe `xi<=chi`, continuity and strict increase below criticality, `xi→0` at zero, `xi→∞` at criticality, and left divergence of susceptibility | `twoPointConnectivity_le_one_sub_susceptibility_inv_pow`, `correlationLength_le_susceptibility`, `correlationLength_continuousOn_subcritical`, `correlationLength_strictMonoOn_subcritical`, `correlationLength_tendsto_zero_at_zero`, `correlationLength_tendsto_top_at_critical`, `susceptibility_tendsto_top_at_critical` | correlation length uses ENNReal; the real zero-endpoint limit uses the explicit clamped density adapter | `cases/CorrelationLength.lean` | not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.75, (6.77), p.132 | exact bound above `n>chi^2`; subcritical exponential decay for sufficiently large `n` | `clusterSizeAtLeast_le_two_exp_neg`, `clusterSizeAtLeast_exponential_decay_of_lt_critical` | the printed all-`n>=1` strict corollary is rejected as false at `n=1`; the corrected eventual theorem is the proved target | `cases/TailAndRates.lean`, `counterexamples/AntiTargets.lean` | not run | source literal `rejected`; corrected theorem `PASS` |
| 6.78, (6.80), (6.82)–(6.83), pp.132–134 | exact-size log rate, exact finite-index prefactor, full-density finite-tail rate, positivity below criticality, `zeta<=phi` | `clusterSizeProbability_logRate_tendsto`, `clusterSizeDecayRate_nonneg`, `finiteClusterSizeProbability_le_rate`, `finiteClusterSizeTail_logRate_tendsto_all`, `clusterSizeDecayRate_pos_of_lt_critical`, `clusterSizeDecayRate_le_boxRadiusDecayRate` | finite tail deliberately excludes infinite clusters and (6.82) holds for every `0<p<1`; ordinary tail is identified only under `theta=0` | `cases/TailAndRates.lean`, independent Comparator challenge | development pass; trusted CI pending | `PASS` |
| 6.87, pp.134–135 | finite nonempty terminal set has a removable terminal preserving connection among the rest | `exists_terminal_deletion_preserves_connected` | the arbitrary-set reading is rejected via the integer-path counterexample; the finite nonempty `Finset` correction is exactly what the proof and later use need | preliminary `Lemmas687689.lean`; `counterexamples/README.md` | not run | source literal `rejected`; corrected theorem `PASS` |
| 6.89, pp.135–136 | three-point probability bounded by a sum of triple two-point products | `threePointConnectivity_le_tsum_prod` | ENNReal sum prevents nonsummable-real-tsum junk at endpoints | preliminary `Lemmas687689.lean` | not run | `PASS_DOCUMENTED_DIVERGENCE` |
| 6.93–6.97, pp.136–138 | general skeleton bound, canonical insertion-code enumeration, moment bound, and literal exponential-moment bound | `multiPointConnectivity_le_skeleton_sum`, `cubicConnectivitySkeleton_card_eq`, `connectivitySkeletonCount_succ`, `connectivitySkeletonCount_eq_doubleFactorial`, `clusterSizeMoment_le`, `clusterSizeWeightedExpMoment_eq_exponentialMoment`, `clusterSizeExponentialMoment_le` | the count is now `Nat.card` of a canonical recursive type; the separate concrete decoder has the same proved cardinality and supplies geometry; the expectation is identified with the summed power series | `cases/TreeMoments.lean`, independent concrete decoder counts `1,3,15` | not run | `PASS_DOCUMENTED_DIVERGENCE`; graph-isomorphism quotient equivalence remains a P2 strengthening |
| 6.102, pp.139–141 | normalized exact probabilities are supermultiplicative with exact factor `p(1-p)^-2` for positive sizes and `0<p<1` | `CubicBondAnimal.finiteClusterSizeProbability_normalized_supermultiplicative` | no change; normalization obtained by rooted/pointed-anchored equivalence | `cases/TailAndRates.lean` | not run | `PASS` |
| 6.108, pp.142–145 | cluster density and susceptibility are real analytic on `[0,p_c)`, via complex animal series agreeing with physical functions | `concreteClusterDensitySeries_analyticOnNhd_belowCritical`, `concreteSusceptibilitySeries_analyticOnNhd_belowCritical`, combined theorem | real `AnalyticOnNhd` includes genuine neighborhoods; endpoint and interior complex majorants are separate | `cases/Analyticity.lean`, endpoint analytic anti-target guard | not run | `PASS_DOCUMENTED_DIVERGENCE` |

The read-only independent rerun passes production statement fidelity and axioms. “Not run” is not
a Comparator pass, so strict protocol status remains inconclusive until a trusted challenge runs.
