# Frozen Chapter 11 source-to-Lean correspondence

Exact declarations are qualified by `Percolation.`.  The natural-language inventory is derived
from Grimmett, *Percolation*, 2nd ed., printed pp. 282–332.  `Challenge.lean` independently
re-states the exact source-facing types for 11.11, 11.12, 11.21, 11.22, 11.24, 11.27, 11.70,
11.75, 11.115, and 11.116; `cases/Chapter11Cases.lean` proves concrete applications.  The production
source locations below contain the complete exact types for the remaining declarations.

| Source | Natural statement and conventions | Exact Lean type / declaration | Dependency and divergence | Cases / adversarial evidence | Axiom verdict | Status |
|---|---|---|---|---|---|---|
| Prop. 11.2, p.284 | A finite connected square subgraph has one surrounding dual boundary circuit.  Geometric uniqueness must quotient base point and reversal. | `existsUnique_boundaryCircuitCrossedEdges (G) : ∃! E : Finset SquareEdge, ∃ c, c.crossedPrimalEdgeFinset = E ∧ G.IsBoundaryCircuit c` in `Planar/External.lean` | Kesten p.386; uniqueness is the crossed-edge finset, not the based walk record. | `Chapter11Infrastructure`; rebasing anti-target in counterexample README. | direct named axiom | external axiom |
| Thm. 11.4, p.285 | `κ(p)=κ(1-p)+1-2p`, with `κ=E(1/|C|)`. | `openClustersPerVertex_square_duality (p : I) : openClustersPerVertex 2 p = openClustersPerVertex 2 (σ p) + 1 - 2*(p:ℝ)` | Kesten face/component correspondence and Euler formula. | production application test. | direct named axiom | external axiom |
| Thm. 11.11, p.287 | Square-lattice bond threshold equals `1/2`. | `cubicCriticalProbability_two_eq_half : cubicCriticalProbability 2 = (1/2:ℝ)` | 11.12, 11.21, RSW and six named topology/Russo axioms; no theorem-specific axiom. | independent challenge and case. | exact set in `axioms.md` | proved modulo external primitives |
| Lem. 11.12, p.288 | Rooted `θ(1/2)=0`. | `theta_two_half_eq_zero : theta 2 squareHalfDensity = 0` | independent annular barriers and RSW. | challenge; critical-density case. | same six named primitives | proved modulo external primitives |
| Lem. 11.13, p.288 | If `θ(p)>0`, each fixed box is almost surely surrounded by a finite open circuit. | `eventuallySurroundingOpenCircuit_probability_one {p} (hθ) (n) : (bernoulliBondMeasure 2 p).real (eventuallySurroundingOpenCircuitEvent n) = 1` | Proposition 11.2 boundary extraction.  Event uses odd face parity for every box vertex. | three-quarter-density application. | direct named axiom | external axiom |
| Lem. 11.21, p.294 | A crossing of `[0,n+1]×[0,n]` has probability `1/2` at density `1/2`, for every natural `n`. | `bernoulliBondMeasure_real_grimmettRectangleCrossingEvent_half (n) : ... = 1/2` | finite trace weights and external dual-trace bijection. | challenge includes `n=0`; general case test. | standard plus trace-equivalence axiom | proved modulo external primitive |
| Lem. 11.22, p.295 | For `p>1/2`, the probability that the maximum number of edge-disjoint literal-rectangle crossings is at most `βn` is at most `e^{-γn}`, for positive constants and `n≥1`. | Full existential type is `Review.Chapter11.lemma_11_22`; production declaration `maxEdgeDisjointGrimmettRectangleCrossings_probability_le_exp`. | Literal rectangle is a direct axiom.  `maxEdgeDisjointSquareRectangleCrossings_probability_le_exp` proves the centered `[0,n+1]×[-n,n]` normalization. | exact source application; README rejects silent rectangle conversion. | literal theorem axiom; centered theorem uses named topology | external literal / proved normalization |
| Thm. 11.24, p.295 | For `1/2<p<1`, the truncated rate exists and `0<ξᶠ(p)<∞`, with `ξᶠ(p)=½ξ(1-p)`. | `truncatedTwoPointConnectivity_logRate_tendsto`; `finiteCorrelationLength_eq_half_correlationLength_complement`; `finiteCorrelationLength_pos_lt_top` in `Chapter11External.lean` | Equality/circuit comparison external; positivity and finiteness are proved from the equality, exact threshold, and Chapter 6 subcritical rate positivity. | three-quarter-density equality and endpoint case. | equality axiom plus its visible dependencies | external equality; consequence proved |
| Thm. 11.25, p.296 | For `1/2<p<1`, exact finite-cluster size probability is bounded by `exp(-η√n)` for positive `η`. | `finiteClusterSizeProbability_le_exp_neg_sqrt_of_supercritical {p} hpHalf hpOne : ∃ η>0, ∀ n, ...` | dual enclosure from Proposition 11.2. | three-quarter-density application. | direct named axiom | external axiom |
| Lem. 11.27, pp.296–297 | Tube log rate exists, is positive and finite, gives the exponential bound, decreases in width, and tends to the unrestricted axis rate. | Exact six-conjunct type is `Review.Chapter11.lemma_11_27`; production `tubeConnectivityDecayRate_properties hp0 hp1 k`. | Production generalizes source `k≥1` to `k=0`; source case is an exact wrapper. | `k=1` challenge/case; checked `k=0` anti-target. | only standard Lean axioms | proved |
| Thm. 11.55, p.304 | If `f(u)/log u→a>0`, a unique `q∈(1/2,1)` is the profile-region threshold and satisfies `ξ(1-q)=a`. | Full `∃! q : I` type is `logarithmicProfileRegion_criticalProbability` in `Chapter11External.lean`. | surrounding dual circuits; `f` nonnegative on nonnegative reals. | exact type/endpoint inspection. | direct named axiom | external axiom |
| Thm. 11.63, p.311 | Bounded cluster-functional sums over growing circuit interiors satisfy the standardized CLT away from `1/2`, given the infinite-cluster and variance hypotheses. | `clusterFunctionalSum_centralLimitTheorem c f p hp0 hp1 hpCritical hc hfBounded hfMeasurable hfInfinite hvariance : SatisfiesCentralLimitTheorem ...` | Book cites external CLT machinery.  Lean adds the implicit measurability hypothesis. | exact type inspection; arbitrary functions are not assumed measurable. | direct named axiom | external axiom with documented specialization |
| Thm. 11.70, p.315 | `r^12(1-√(1-r))^48` is a lower bound for the annular open-circuit probability, for `l≥1`. | Exact type `Review.Chapter11.theorem_11_70`; production `rswAnnulusOpenCircuitProbability_ge`. | 11.73 and 11.75; exact constants retained. | challenge; `l=0` event sanity is independently checked but source theorem uses `l≥1`. | three gluing axioms plus 11.73 axiom | proved modulo external primitives |
| Lem. 11.73, p.316 | Lowest-crossing argument gives the `3l×2l` bound `(1-√(1-r))^3`. | `rswThreeHalvesCrossingProbability_ge (p) (l) : ...` in `External.lean` | Russo 1981 lowest-crossing input.  The axiom is unguarded at `l=0`; the degenerate event is independently proved `univ`. | checked `l=0` in counterexamples. | direct named axiom | external axiom |
| Lem. 11.75, p.317 | Three FKG gluing inequalities (11.76)–(11.78). | Exact three-conjunct type `Review.Chapter11.lemma_11_75`; production `rsw_gluing_inequalities`. | FKG plus three isolated deterministic event inclusions. | challenge and production application. | only three named gluing axioms plus standards | proved modulo external primitives |
| Thm. 11.89, p.324 | Critical radius/size tails have the stated two-sided powers and some positive cluster-size moment is integrable. | Full constants-and-`Integrable` type is `squareCritical_powerLaw_bounds` in `Chapter11External.lean`. | 11.70/11.73 and Proposition 11.2. | production application checks all quantifier order. | direct named axiom | external axiom |
| Thm. 11.93, p.325 | Near-critical power bounds hold for `θ`, `χ`, and finite susceptibility on the strict sides of `1/2`. | `squareNearCritical_powerLaw_bounds` in `Chapter11External.lean`. | 11.89; strict phases and real conversions explicit. | exact type inspection. | direct named axiom | external axiom |
| Thm. 11.115, p.332 | For `p_h,p_v<1`, rooted square `θ` vanishes on `p_h+p_v≤1` and is positive above it. | Exact two-implication type `Review.Chapter11.theorem_11_115`; production `inhomogeneousSquare_criticalSurface hpₕ hpᵥ`. | Original inhomogeneous result.  Rooted origin-cluster event, not unrooted existence. | `(1/4,3/4)` and `(3/4,3/4)` cases; `p=1` anti-target. | wrapper over two named axioms | proved wrapper |
| Thm. 11.116, p.332 | For three densities below one, rooted triangular `θ` is separated by `p_h+p_v+p_d-p_hp_vp_d=1`. | Exact two-implication type `Review.Chapter11.theorem_11_116`; production `inhomogeneousTriangular_criticalSurface`. | Original star–triangle result; graph adds north-east diagonals. | half-density polynomial oracle and positive-phase case. | wrapper over two named axioms | proved wrapper |

## Numbered supporting results

The grouped source-first inventory for every numbered display 11.1–11.116 is in
`audit/topics/topic-11-bond-percolation-two-dimensions.md`.  No additional named theorem is hidden
in those groups.  The OCR text contains no separately labelled 11.102; that fact is recorded
rather than silently inventing a source result.

## Telemetry

Per-theorem construction windows were not consistently snapshotted.  They are therefore recorded
as **not individually measured** in the topic table.  The active goal tracker provides only an
aggregate measured total; no reconstructed allocation is presented as measured telemetry.
